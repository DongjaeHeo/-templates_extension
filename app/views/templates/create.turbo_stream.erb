
<%= turbo_stream.replace 'template_menu', partial: 'workspaces/template_menu', locals: { categories: current_user.categories } %>
<%= turbo_stream.replace 'workspace_content', partial: 'templates/form', locals: { template: @template, category: @template.category } %>
